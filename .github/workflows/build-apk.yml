name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.24.5'
          cache: true

      - name: Inject google-services.json
        env:
          GCS_B64: ${{ secrets.GOOGLESERVICES_BASE64 }}
        run: |
          if [ -z "$GCS_B64" ]; then
            echo "::error::GOOGLESERVICES_BASE64 secret is missing"
            exit 1
          fi

          printf '%s' "$GCS_B64" | tr -d '[:space:]' | base64 -d > android/app/google-services.json

          if [ ! -s android/app/google-services.json ]; then
            echo "::error::google-services.json could not be created"
            exit 1
          fi

          echo "google-services.json created successfully"
          ls -lh android/app/google-services.json

      - name: Flutter pub get
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: mess-management-apk
          path: build/app/outputs/flutter-apk/*.apk
